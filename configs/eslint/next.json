{
  "extends": ["next", "./biome"],
  "overrides": [
    {
      "extends": ["biome"],
      "files": ["*.ts", "*.js", "*.tsx", "*.jsx"],
      "rules": {
        "no-unused-vars": "off",
        "react/display-name": "off"
      }
    }
  ]
}
